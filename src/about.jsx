'use strict';

import React from "react";

const About = () => {
    return (
        <div className="about-container">
            There's nothing about us, we aren't a real company!
        </div>
    );
};

export default About;