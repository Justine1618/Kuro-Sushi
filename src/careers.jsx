'use strict';

import React from "react";

const Careers = () => {
    return (
        <div className="careers-container">
            You can't work here, we aren't a real company!
        </div>
    );
};

export default Careers;